import { Options } from "prettier";

const config: Options = {
    endOfLine: "lf",
    printWidth: 80,
    tabWidth: 6,
    trailingComma: "es5",
    singleQuote: false,
    jsxSingleQuote: false,
    semi: true,
};

export = config;
